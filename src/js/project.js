const swiper1 = new Swiper('.swiper1', {
    // Optional parameters
        loop: true,
        slidesPerView:3,
        slidesPerGroup:1,
        spaceBetween:25,
    // If we need pagination
    pagination: {
      el: '.swiper-pagination',
    },
  
    });

